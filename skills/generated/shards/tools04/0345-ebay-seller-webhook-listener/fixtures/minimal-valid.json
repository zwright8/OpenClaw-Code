{
  "skillId": 345,
  "skillName": "ebay-seller-webhook-listener",
  "title": "eBay Seller Webhook Listener",
  "provider": "eBay Seller",
  "category": "External SaaS integrations",
  "operation": "webhook-listener",
  "authContext": {
    "mode": "oauth2",
    "envVars": [
      "EBAY_SELLER_CLIENT_ID",
      "EBAY_SELLER_CLIENT_SECRET"
    ],
    "status": "present-and-validated"
  },
  "input": {
    "requiredSignals": {
      "signed_event_payload": "sample_signed_event_payload",
      "secret_reference": "sample_secret_reference",
      "routing_rules": "sample_routing_rules"
    },
    "approvalGates": [
      "signing-secret-check",
      "handler-policy-review"
    ],
    "rollbackStrategy": "pause-listener-and-replay-from-quarantine-queue"
  },
  "expected": {
    "primaryArtifact": "ebay-seller-event-intake-log",
    "kpiFocus": [
      "signature verification pass rate",
      "event processing latency",
      "duplicate suppression accuracy"
    ],
    "validationSuites": [
      "unit",
      "integration",
      "simulation",
      "regression-baseline"
    ],
    "postureThresholds": {
      "readyMin": 78,
      "reviewMin": 58,
      "reviewRisk": 56,
      "criticalRisk": 75
    }
  },
  "regressionHint": {
    "scenario": "Webhook delivery with an invalid signature and a duplicate event ID already present in the dedupe cache.",
    "mutation": "Replace the signing secret with an incorrect value and reuse the same event identifier in the fixture."
  }
}
