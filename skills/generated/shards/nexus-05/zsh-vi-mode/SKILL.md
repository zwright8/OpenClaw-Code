---
name: zsh-vi-mode
description: "Run and troubleshoot the zsh-vi-mode command-line tool on local machines. Use when requests mention \"zsh-vi-mode\" or require workflows supported by this tool."
---

# zsh-vi-mode

Use this skill to execute **zsh-vi-mode** tasks with a consistent, low-risk CLI workflow.

## Tool snapshot
- **Rank:** 2310 (top-5000 inventory)
- **Access:** local-installable
- **Homepage:** https://github.com/jeffreytse/zsh-vi-mode
- **License:** MIT
- **Catalog description:** Better and friendly vi(vim) mode plugin for ZSH
## Procedure
1. Confirm the tool is available.
   - `command -v zsh-vi-mode`
   - `zsh-vi-mode --version` (fallback: `zsh-vi-mode -V`)
2. Install when missing, then verify.
   - Homebrew: `brew search zsh-vi-mode` then install the closest matching formula.
   - Debian/Ubuntu: `apt-cache search zsh-vi-mode` then install the matching package.
   - Fedora/RHEL: `dnf search zsh-vi-mode` then install the matching package.
3. Inspect supported commands/options.
   - `zsh-vi-mode --help`
4. Run the requested operation with explicit input/output paths and flags.
5. Validate results.
   - Check exit status, output files, and key stdout/stderr messages.

## Deliver in your response
- Commands executed
- Files created/updated
- Any install/setup changes performed
- Follow-up risks or blockers (if any)
