interface:
  display_name: "Regression Sentinel Design for product management execution"
  short_description: "Regression sentinel design for product management execution with guardrails and measurable outcomes."
  default_prompt: "Use Regression Sentinel Design for product management execution to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
