interface:
  display_name: "PublicService Privacy Preserving Data Broker"
  short_description: "Policy-scoped data mediation for Healthcare and Public Services with guardrails and measurable outcomes."
  default_prompt: "Use PublicService Privacy Preserving Data Broker to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
