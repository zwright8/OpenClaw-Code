interface:
  display_name: "Collab Regression Sentinel"
  short_description: "Baseline-delta detection for Collaboration and Negotiation with guardrails and measurable outcomes."
  default_prompt: "Use Collab Regression Sentinel to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
