interface:
  display_name: "Logistics Regression Sentinel"
  short_description: "Baseline-delta detection for Resource Allocation and Logistics with guardrails and measurable outcomes."
  default_prompt: "Use Logistics Regression Sentinel to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
