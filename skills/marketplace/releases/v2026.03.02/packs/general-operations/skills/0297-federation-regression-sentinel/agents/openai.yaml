interface:
  display_name: "Federation Regression Sentinel"
  short_description: "Baseline-delta detection for Federation and Interoperability with guardrails and measurable outcomes."
  default_prompt: "Use Federation Regression Sentinel to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
