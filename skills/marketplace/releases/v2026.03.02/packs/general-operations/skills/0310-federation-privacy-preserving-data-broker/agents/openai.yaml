interface:
  display_name: "Federation Privacy Preserving Data Broker"
  short_description: "Policy-scoped data mediation for Federation and Interoperability with guardrails and measurable outcomes."
  default_prompt: "Use Federation Privacy Preserving Data Broker to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
