interface:
  display_name: "Oversight Regression Sentinel"
  short_description: "Baseline-delta detection for Human Oversight and Operator UX with guardrails and measurable outcomes."
  default_prompt: "Use Oversight Regression Sentinel to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
