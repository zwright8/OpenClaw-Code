interface:
  display_name: "Logistics Risk-Aware Scheduler"
  short_description: "Risk-weighted sequencing for Resource Allocation and Logistics with guardrails and measurable outcomes."
  default_prompt: "Use Logistics Risk-Aware Scheduler to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
