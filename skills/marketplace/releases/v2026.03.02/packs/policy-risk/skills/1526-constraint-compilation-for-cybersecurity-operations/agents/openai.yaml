interface:
  display_name: "Constraint Compilation for cybersecurity operations"
  short_description: "Constraint compilation for cybersecurity operations with guardrails and measurable outcomes."
  default_prompt: "Use Constraint Compilation for cybersecurity operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
