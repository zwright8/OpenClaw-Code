interface:
  display_name: "Failure Root Cause Mining for healthcare operations"
  short_description: "Failure root cause mining for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Failure Root Cause Mining for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
