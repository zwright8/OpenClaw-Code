interface:
  display_name: "Resource Budget Allocation for education support services"
  short_description: "Resource budget allocation for education support services with guardrails and measurable outcomes."
  default_prompt: "Use Resource Budget Allocation for education support services to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
