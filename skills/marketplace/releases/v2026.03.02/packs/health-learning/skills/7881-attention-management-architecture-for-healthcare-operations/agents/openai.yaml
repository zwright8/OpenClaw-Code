interface:
  display_name: "Attention Management Architecture for healthcare operations"
  short_description: "Attention management architecture for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Attention Management Architecture for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
