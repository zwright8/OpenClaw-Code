interface:
  display_name: "Attention Management Architecture for education support s..."
  short_description: "Attention management architecture for education support services with guardrails and measurable outcomes."
  default_prompt: "Use Attention Management Architecture for education support services to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
