interface:
  display_name: "Human Approval Routing for healthcare operations"
  short_description: "Human approval routing for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Human Approval Routing for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
