interface:
  display_name: "Risk-Aware Scheduling for healthcare operations"
  short_description: "Risk-aware scheduling for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Risk-Aware Scheduling for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
