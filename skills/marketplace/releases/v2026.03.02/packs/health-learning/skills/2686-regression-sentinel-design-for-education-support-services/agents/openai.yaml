interface:
  display_name: "Regression Sentinel Design for education support services"
  short_description: "Regression sentinel design for education support services with guardrails and measurable outcomes."
  default_prompt: "Use Regression Sentinel Design for education support services to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
