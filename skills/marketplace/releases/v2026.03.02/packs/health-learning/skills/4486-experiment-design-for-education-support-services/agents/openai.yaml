interface:
  display_name: "Experiment Design for education support services"
  short_description: "Experiment design for education support services with guardrails and measurable outcomes."
  default_prompt: "Use Experiment Design for education support services to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
