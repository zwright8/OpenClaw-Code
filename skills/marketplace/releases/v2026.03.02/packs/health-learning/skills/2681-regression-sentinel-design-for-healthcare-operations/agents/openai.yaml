interface:
  display_name: "Regression Sentinel Design for healthcare operations"
  short_description: "Regression sentinel design for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Regression Sentinel Design for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
