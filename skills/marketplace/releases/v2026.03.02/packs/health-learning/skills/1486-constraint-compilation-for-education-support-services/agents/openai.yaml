interface:
  display_name: "Constraint Compilation for education support services"
  short_description: "Constraint compilation for education support services with guardrails and measurable outcomes."
  default_prompt: "Use Constraint Compilation for education support services to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
