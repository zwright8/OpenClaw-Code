interface:
  display_name: "KPI Dashboard Publishing for healthcare operations"
  short_description: "Kpi dashboard publishing for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use KPI Dashboard Publishing for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
