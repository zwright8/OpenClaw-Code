interface:
  display_name: "Resource Budget Allocation for healthcare operations"
  short_description: "Resource budget allocation for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Resource Budget Allocation for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
