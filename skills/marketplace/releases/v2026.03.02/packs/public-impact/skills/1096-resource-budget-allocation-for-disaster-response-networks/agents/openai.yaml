interface:
  display_name: "Resource Budget Allocation for disaster response networks"
  short_description: "Resource budget allocation for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Resource Budget Allocation for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
