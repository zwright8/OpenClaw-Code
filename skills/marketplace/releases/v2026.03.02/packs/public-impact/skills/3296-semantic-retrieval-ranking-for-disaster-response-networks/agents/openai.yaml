interface:
  display_name: "Semantic Retrieval Ranking for disaster response networks"
  short_description: "Semantic retrieval ranking for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Semantic Retrieval Ranking for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
