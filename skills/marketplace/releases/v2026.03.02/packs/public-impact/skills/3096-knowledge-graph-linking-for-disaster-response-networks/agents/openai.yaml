interface:
  display_name: "Knowledge Graph Linking for disaster response networks"
  short_description: "Knowledge graph linking for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Knowledge Graph Linking for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
