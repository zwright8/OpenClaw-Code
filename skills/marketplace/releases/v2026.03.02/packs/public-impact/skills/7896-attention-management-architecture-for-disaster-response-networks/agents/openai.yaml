interface:
  display_name: "Attention Management Architecture for disaster response n..."
  short_description: "Attention management architecture for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Attention Management Architecture for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
