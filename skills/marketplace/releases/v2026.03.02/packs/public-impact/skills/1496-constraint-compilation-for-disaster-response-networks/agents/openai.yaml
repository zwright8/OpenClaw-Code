interface:
  display_name: "Constraint Compilation for disaster response networks"
  short_description: "Constraint compilation for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Constraint Compilation for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
