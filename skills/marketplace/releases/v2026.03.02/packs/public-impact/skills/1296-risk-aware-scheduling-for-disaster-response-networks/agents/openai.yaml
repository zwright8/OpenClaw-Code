interface:
  display_name: "Risk-Aware Scheduling for disaster response networks"
  short_description: "Risk-aware scheduling for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Risk-Aware Scheduling for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
