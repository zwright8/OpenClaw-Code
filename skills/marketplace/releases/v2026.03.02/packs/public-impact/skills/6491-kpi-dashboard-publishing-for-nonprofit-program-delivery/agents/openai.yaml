interface:
  display_name: "KPI Dashboard Publishing for nonprofit program delivery"
  short_description: "Kpi dashboard publishing for nonprofit program delivery with guardrails and measurable outcomes."
  default_prompt: "Use KPI Dashboard Publishing for nonprofit program delivery to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
