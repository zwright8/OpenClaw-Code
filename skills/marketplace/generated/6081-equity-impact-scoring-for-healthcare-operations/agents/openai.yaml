interface:
  display_name: "Equity Impact Scoring for healthcare operations"
  short_description: "Equity impact scoring for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Equity Impact Scoring for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
