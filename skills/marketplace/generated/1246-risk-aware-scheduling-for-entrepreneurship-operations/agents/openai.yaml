interface:
  display_name: "Risk-Aware Scheduling for entrepreneurship operations"
  short_description: "Risk-aware scheduling for entrepreneurship operations with guardrails and measurable outcomes."
  default_prompt: "Use Risk-Aware Scheduling for entrepreneurship operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
