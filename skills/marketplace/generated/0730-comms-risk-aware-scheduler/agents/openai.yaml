interface:
  display_name: "Comms Risk-Aware Scheduler"
  short_description: "Risk-weighted sequencing for Communication and Explainability with guardrails and measurable outcomes."
  default_prompt: "Use Comms Risk-Aware Scheduler to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
