interface:
  display_name: "Retry And Backoff Orchestration for healthcare operations"
  short_description: "Retry and backoff orchestration for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Retry And Backoff Orchestration for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
