interface:
  display_name: "KPI Dashboard Publishing for entrepreneurship operations"
  short_description: "Kpi dashboard publishing for entrepreneurship operations with guardrails and measurable outcomes."
  default_prompt: "Use KPI Dashboard Publishing for entrepreneurship operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
