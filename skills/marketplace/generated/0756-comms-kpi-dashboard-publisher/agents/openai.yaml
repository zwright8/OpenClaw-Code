interface:
  display_name: "Comms KPI Dashboard Publisher"
  short_description: "Metric synthesis and publication for Communication and Explainability with guardrails and measurable outcomes."
  default_prompt: "Use Comms KPI Dashboard Publisher to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
