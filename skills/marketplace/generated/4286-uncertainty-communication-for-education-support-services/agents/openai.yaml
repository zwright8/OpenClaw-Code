interface:
  display_name: "Uncertainty Communication for education support services"
  short_description: "Uncertainty communication for education support services with guardrails and measurable outcomes."
  default_prompt: "Use Uncertainty Communication for education support services to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
