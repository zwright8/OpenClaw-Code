interface:
  display_name: "Uncertainty Communication for disaster response networks"
  short_description: "Uncertainty communication for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Uncertainty Communication for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
