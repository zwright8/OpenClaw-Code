interface:
  display_name: "Uncertainty Communication for healthcare operations"
  short_description: "Uncertainty communication for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Uncertainty Communication for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
