interface:
  display_name: "Regression Sentinel Design for disaster response networks"
  short_description: "Regression sentinel design for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Regression Sentinel Design for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
