interface:
  display_name: "Human Approval Routing for disaster response networks"
  short_description: "Human approval routing for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Human Approval Routing for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
