interface:
  display_name: "Constraint Compilation for healthcare operations"
  short_description: "Constraint compilation for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Constraint Compilation for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
