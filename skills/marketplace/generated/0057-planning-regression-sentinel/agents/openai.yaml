interface:
  display_name: "Planning Regression Sentinel"
  short_description: "Baseline-delta detection for Strategic Planning and Decomposition with guardrails and measurable outcomes."
  default_prompt: "Use Planning Regression Sentinel to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
