interface:
  display_name: "Failure Root Cause Mining for disaster response networks"
  short_description: "Failure root cause mining for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Failure Root Cause Mining for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
