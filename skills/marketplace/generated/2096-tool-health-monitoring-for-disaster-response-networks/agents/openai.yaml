interface:
  display_name: "Tool Health Monitoring for disaster response networks"
  short_description: "Tool health monitoring for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use Tool Health Monitoring for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
