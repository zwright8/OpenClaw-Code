interface:
  display_name: "KPI Dashboard Publishing for disaster response networks"
  short_description: "Kpi dashboard publishing for disaster response networks with guardrails and measurable outcomes."
  default_prompt: "Use KPI Dashboard Publishing for disaster response networks to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
