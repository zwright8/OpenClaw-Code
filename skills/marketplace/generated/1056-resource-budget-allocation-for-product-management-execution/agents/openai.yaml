interface:
  display_name: "Resource Budget Allocation for product management execution"
  short_description: "Resource budget allocation for product management execution with guardrails and measurable outcomes."
  default_prompt: "Use Resource Budget Allocation for product management execution to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
