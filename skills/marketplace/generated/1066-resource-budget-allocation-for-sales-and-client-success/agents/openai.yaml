interface:
  display_name: "Resource Budget Allocation for sales and client success"
  short_description: "Resource budget allocation for sales and client success with guardrails and measurable outcomes."
  default_prompt: "Use Resource Budget Allocation for sales and client success to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
