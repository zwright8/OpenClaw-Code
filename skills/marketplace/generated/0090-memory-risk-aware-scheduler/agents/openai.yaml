interface:
  display_name: "Memory Risk-Aware Scheduler"
  short_description: "Risk-weighted sequencing for Memory and Knowledge Operations with guardrails and measurable outcomes."
  default_prompt: "Use Memory Risk-Aware Scheduler to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
