interface:
  display_name: "Plan Quality Evaluation for healthcare operations"
  short_description: "Plan quality evaluation for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Plan Quality Evaluation for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
