interface:
  display_name: "Memory Consolidation for scientific publishing pipelines"
  short_description: "Memory consolidation for scientific publishing pipelines with guardrails and measurable outcomes."
  default_prompt: "Use Memory Consolidation for scientific publishing pipelines to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
