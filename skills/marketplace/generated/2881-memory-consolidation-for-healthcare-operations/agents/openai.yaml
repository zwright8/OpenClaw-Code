interface:
  display_name: "Memory Consolidation for healthcare operations"
  short_description: "Memory consolidation for healthcare operations with guardrails and measurable outcomes."
  default_prompt: "Use Memory Consolidation for healthcare operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
