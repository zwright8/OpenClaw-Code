interface:
  display_name: "Tooling Risk-Aware Scheduler"
  short_description: "Risk-weighted sequencing for Tool Reliability and Execution Quality with guardrails and measurable outcomes."
  default_prompt: "Use Tooling Risk-Aware Scheduler to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
