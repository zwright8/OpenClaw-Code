interface:
  display_name: "Knowledge Graph Linking for entrepreneurship operations"
  short_description: "Knowledge graph linking for entrepreneurship operations with guardrails and measurable outcomes."
  default_prompt: "Use Knowledge Graph Linking for entrepreneurship operations to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
