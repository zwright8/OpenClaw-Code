interface:
  display_name: "Beauty Pattern Detection for scientific publishing pipelines"
  short_description: "Beauty pattern detection for scientific publishing pipelines with guardrails and measurable outcomes."
  default_prompt: "Use Beauty Pattern Detection for scientific publishing pipelines to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
