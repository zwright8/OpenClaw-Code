interface:
  display_name: "Rights Risk-Aware Scheduler"
  short_description: "Risk-weighted sequencing for Legal, Rights, and Compliance with guardrails and measurable outcomes."
  default_prompt: "Use Rights Risk-Aware Scheduler to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
