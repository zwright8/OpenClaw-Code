interface:
  display_name: "Collab Risk-Aware Scheduler"
  short_description: "Risk-weighted sequencing for Collaboration and Negotiation with guardrails and measurable outcomes."
  default_prompt: "Use Collab Risk-Aware Scheduler to scope the task, execute the workflow, enforce guardrails, and return a concrete outcome artifact with next actions."
