{
  "name": "swarm-protocol",
  "version": "0.1.0",
  "description": "Standardized communication schemas for OpenClaw agent swarms",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "tsx --test && tsx run-tests.ts",
    "test:unit": "tsx --test",
    "test:legacy": "tsx run-tests.ts",
    "demo:orchestrator": "tsx scripts/orchestrator-demo.ts",
    "approval:queue": "tsx scripts/export-approval-queue.ts --json state/approval-queue.json --markdown state/approval-queue.md",
    "benchmark:simulate": "tsx scripts/run-simulation-benchmark.ts --scenario scenarios/baseline.json --runs 8 --thresholds scenarios/baseline-thresholds.json --json state/simulation-benchmark.json --markdown state/simulation-benchmark.md",
    "ops": "tsx scripts/swarm-ops.ts",
    "build": "npm test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "uuid": "^13.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^24.5.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
