# AGENTS.md

## Verified Workflows (README-grounded)
- `npm run capabilities:blueprint` - refresh capability blueprint artifacts.
- `npm run capabilities:audit` - run deployability audit artifacts.
- `npm run skills:sync` - execute the 1000-skill lifecycle pipeline.
- `npm run skills:improve:10000` - generate the 10k-skill improvement catalog.
- `npm run skills:harden:profile` - build hardening/deployability profile artifacts.
- `npm run skills:harden` - apply hardening and emit deployability summaries.
- `npm run skills:marketplace:build` - build marketplace skill packages.
- `npm run skills:marketplace:validate` - validate marketplace skill packages.
- `npm run skills:marketplace:analytics` - generate marketplace analytics artifacts.
- `npm run skills:marketplace:release` - package marketplace release artifacts.
- `npm run skills:marketplace:release:validate` - validate packaged marketplace release artifacts.
- `npm run skills:marketplace:ship` - run the end-to-end marketplace packaging chain.
- `npm run skills:marketplace:v2:build` - build marketplace v2 skill packages.
- `npm run skills:marketplace:v2:validate` - validate marketplace v2 skill packages.
- `npm run skills:marketplace:v2:demo` - run top-package demo for v2 outputs.
- `npm run worker:loop -- --deploy-index ../skills/state/skills.deployability.index.json --hardening-profile ../skills/state/skills.hardening.profile.json` - run worker loop with hardening inputs.
- `npm run autonomous:run -- --deploy-index ../skills/state/skills.deployability.index.json --hardening-profile ../skills/state/skills.hardening.profile.json` - run autonomous loop with hardening inputs.
- `npm run autonomous:run -- --selection-policy linucb --linucb-alpha 0.6` - run autonomous loop with contextual LinUCB exploration using learned execution-state features.
- `npm run autonomous:run -- --selection-policy sw_linucb --window-size 12 --linucb-alpha 0.6` - run autonomous loop with sliding-window contextual LinUCB adaptation focused on recent execution drift.
- `npm run autonomous:run -- --selection-policy d_linucb --discount-factor 0.97 --linucb-alpha 0.6` - run autonomous loop with discounted contextual LinUCB to adapt faster under drift.
- `npm run autonomous:run -- --selection-policy adwin_linucb --cd-min-samples 8 --adwin-delta 0.002 --linucb-alpha 0.6` - run autonomous loop with ADWIN-trimmed contextual LinUCB to drop stale observations after drift detection.
- `npm run autonomous:run -- --selection-policy ucb_v --ucb-v-exploration 1` - run autonomous loop with variance-aware UCB exploration bonuses for bounded binary outcomes.
- `npm run autonomous:run -- --selection-policy sw_ucb_v --window-size 12 --ucb-v-exploration 1` - run autonomous loop with sliding-window UCB-V adaptation for recency drift.
- `npm run autonomous:run -- --selection-policy mv_ucb --risk-variance-weight 0.6` - run autonomous loop with mean-variance UCB to penalize volatile outcomes while preserving optimism.
- `npm run autonomous:run -- --selection-policy sw_mv_ucb --window-size 12 --risk-variance-weight 0.6` - run autonomous loop with sliding-window mean-variance UCB adaptation focused on recent volatility shifts.
- `npm run autonomous:run -- --selection-policy d_mv_ucb --discount-factor 0.97 --risk-variance-weight 0.6` - run autonomous loop with discounted mean-variance UCB to emphasize recency while damping high-variance arms.
- `npm run autonomous:run -- --selection-policy lints --lints-alpha 0.5` - run autonomous loop with contextual linear Thompson sampling for posterior-driven exploration.
- `npm run autonomous:run -- --selection-policy sw_lints --window-size 12 --lints-alpha 0.5` - run autonomous loop with sliding-window linear Thompson sampling for recency-weighted contextual exploration.
- `npm run autonomous:run -- --selection-policy d_lints --discount-factor 0.97 --lints-alpha 0.5` - run autonomous loop with discounted linear Thompson sampling to adapt under drift.
- `npm run autonomous:run -- --selection-policy adwin_lints --cd-min-samples 8 --adwin-delta 0.002 --lints-alpha 0.5` - run autonomous loop with ADWIN-trimmed linear Thompson sampling to emphasize post-drift contextual evidence.
- `npm run autonomous:run -- --selection-policy epsilon_ts --thompson-exploration 0.35 --thompson-prior-alpha 1 --thompson-prior-beta 1` - run autonomous loop with epsilon-Thompson exploration controls.
- `npm run autonomous:run -- --selection-policy epsilon_ts --thompson-exploration 0.25 --thompson-prior-alpha 1 --thompson-prior-beta 1 --thompson-meta-prior-strength 24` - run autonomous loop with empirical-Bayes Thompson prior warm-start to reduce cold-start variance.
- `npm run autonomous:run -- --selection-policy tt_epsilon_ts --thompson-exploration 0.25 --thompson-top-two-probability 0.8 --thompson-prior-alpha 1 --thompson-prior-beta 1` - run autonomous loop with top-two Thompson leader/challenger routing to reduce posterior lock-in and probe near-best alternatives.
- `npm run autonomous:run -- --selection-policy tt_bb_ts --thompson-exploration 0.25 --thompson-top-two-probability 0.8` - run autonomous loop with top-two Bayesian-bootstrap Thompson leader/challenger routing for bounded non-binary reward exploration.
- `npm run autonomous:run -- --selection-policy bb_ts --thompson-exploration 0.25` - run autonomous loop with Bayesian-bootstrap Thompson sampling for bounded non-binary rewards.
- `npm run autonomous:run -- --selection-policy auto_epsilon_ts --thompson-exploration 0.15 --thompson-uncertainty-weight 0.8 --thompson-prior-alpha 1 --thompson-prior-beta 1` - run autonomous loop with uncertainty-adaptive epsilon-Thompson exploration.
- `npm run autonomous:run -- --selection-policy cp_epsilon_ts --thompson-hazard-rate 0.1 --thompson-surprise-sensitivity 2 --thompson-exploration 0.2` - run autonomous loop with surprise-adaptive changepoint epsilon-Thompson resets for abrupt drift.
- `npm run autonomous:run -- --selection-policy cd_epsilon_ts --cd-min-samples 8 --cd-threshold 1.5 --cd-delta 0.02 --thompson-exploration 0.25` - run autonomous loop with Page-Hinkley change-detection epsilon-Thompson adaptation for abrupt drift.
- `npm run autonomous:run -- --selection-policy sw_cd_epsilon_ts --window-size 12 --cd-min-samples 8 --cd-threshold 1.5 --cd-delta 0.02 --thompson-exploration 0.25` - run autonomous loop with sliding-window Page-Hinkley epsilon-Thompson adaptation emphasizing recent abrupt drift.
- `npm run autonomous:run -- --selection-policy cusum_epsilon_ts --cd-min-samples 8 --cusum-threshold 1.2 --cusum-baseline-weight 0.15 --thompson-exploration 0.25` - run autonomous loop with CUSUM-based epsilon-Thompson adaptation for abrupt drift.
- `npm run autonomous:run -- --selection-policy sw_cusum_epsilon_ts --window-size 12 --cd-min-samples 8 --cusum-threshold 1.2 --cusum-baseline-weight 0.15 --thompson-exploration 0.25` - run autonomous loop with sliding-window CUSUM epsilon-Thompson adaptation to prioritize recent abrupt drift shifts.
- `npm run autonomous:run -- --selection-policy sw_epsilon_ts --window-size 12 --thompson-exploration 0.25` - run autonomous loop with sliding-window epsilon-Thompson exploration controls.
- `npm run autonomous:run -- --selection-policy sw_bb_ts --window-size 12 --thompson-exploration 0.25` - run autonomous loop with sliding-window Bayesian-bootstrap Thompson sampling for recency-sensitive reward shifts.
- `npm run autonomous:run -- --selection-policy sw_auto_epsilon_ts --window-size 12 --thompson-exploration 0.15 --thompson-uncertainty-weight 0.8` - run autonomous loop with sliding-window uncertainty-adaptive epsilon-Thompson exploration.
- `npm run autonomous:run -- --selection-policy sw_cp_epsilon_ts --window-size 12 --thompson-hazard-rate 0.1 --thompson-surprise-sensitivity 2 --thompson-exploration 0.2` - run autonomous loop with sliding-window surprise-adaptive changepoint epsilon-Thompson resets.
- `npm run autonomous:run -- --selection-policy fdsw_epsilon_ts --window-size 12 --discount-factor 0.97 --hybrid-ts-aggregation mean --thompson-exploration 0.25` - run autonomous loop with hybrid discounted+sliding-window epsilon-Thompson adaptation for mixed drift regimes.
- `npm run autonomous:run -- --selection-policy fdsw_ucb --window-size 12 --discount-factor 0.97 --hybrid-ts-aggregation mean` - run autonomous loop with hybrid discounted+sliding-window UCB adaptation for mixed drift regimes.
- `npm run autonomous:run -- --selection-policy fdsw_epsilon_ts --window-size 12 --discount-factor 0.97 --hybrid-ts-aggregation adaptive --thompson-exploration 0.25` - run autonomous loop with drift-aware adaptive blending between sliding-window and discounted epsilon-Thompson statistics.
- `npm run autonomous:run -- --selection-policy fdsw_ucb --window-size 12 --discount-factor 0.97 --hybrid-ts-aggregation adaptive` - run autonomous loop with drift-aware adaptive blending between sliding-window and discounted UCB statistics.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97` - run autonomous loop with discounted UCB adaptation to emphasize recent outcomes under drift.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --max-feedback-delay-ms 120000` - run autonomous loop with delayed-feedback gating so excessively delayed outcomes are skipped from ranking updates.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --feedback-delay-decay-ms 120000` - run autonomous loop with exponential delayed-feedback decay so stale outcomes are down-weighted instead of fully discarded.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --latency-penalty-weight 0.4 --latency-target-ms 120000` - run autonomous loop with discounted UCB plus latency-aware reward shaping to down-weight slow task completions.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --latency-penalty-weight 0.4 --latency-target-ms 120000 --latency-auto-target --latency-auto-target-percentile 0.9 --latency-auto-target-min-samples 8 --latency-auto-target-window-size 32 --latency-auto-target-blend 0.5` - run autonomous loop with discounted UCB latency-aware shaping plus recency-windowed percentile auto-targeting and blended stabilization against the static latency target.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --reliability-floor 0.7 --reliability-floor-min-attempts 8` - run autonomous loop with conservative Wilson-confidence reliability penalties so low-sample lucky streaks do not outrank consistently reliable skills.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --latency-sla-ms 120000 --latency-sla-floor 0.85 --latency-sla-min-attempts 8` - run autonomous loop with conservative Wilson-confidence deadline-hit penalties so consistently late skills do not outrank fast-enough alternatives.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --latency-target-ms 120000 --latency-tail-penalty-weight 0.3 --latency-tail-percentile 0.95 --latency-tail-min-samples 8` - run autonomous loop with discounted UCB percentile tail-latency penalties so high tail-risk skills are deprioritized even when average success is high.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --latency-target-ms 120000 --latency-cvar-penalty-weight 0.35 --latency-cvar-percentile 0.95 --latency-cvar-min-samples 8` - run autonomous loop with discounted UCB CVaR tail-latency penalties so candidates with severe worst-case latency tails are deprioritized.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --failure-burst-penalty-weight 0.45 --failure-burst-short-window 8 --failure-burst-long-window 32 --failure-burst-min-attempts 8 --failure-burst-threshold 1.5` - run autonomous loop with short-vs-long window failure-burst penalties so abruptly regressing skills are temporarily deprioritized.
- `npm run autonomous:run -- --selection-policy d_ucb --discount-factor 0.97 --latency-sla-ms 120000 --latency-burst-penalty-weight 0.45 --latency-burst-short-window 8 --latency-burst-long-window 32 --latency-burst-min-attempts 8 --latency-burst-threshold 1.5` - run autonomous loop with short-vs-long latency-SLA miss burst penalties so abruptly slowing skills are temporarily deprioritized.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-retry-jitter-strategy decorrelated` - run autonomous loop with transient-failure bot retries using decorrelated jitter backoff to reduce synchronized retry bursts.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-retry-hint-max-ms 120000` - run autonomous loop with bounded Retry-After hint honoring so upstream rate-limit guidance influences retry delays without unbounded stalls.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-retry-hint-max-ms 120000 --bot-retry-hint-jitter 0.1` - run autonomous loop with bounded Retry-After/RateLimit-Reset honoring plus minimum-preserving jittered hint delays to reduce synchronized resume bursts after shared cooldown windows.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-retry-hint-max-ms 120000 --bot-retry-hint-jitter 0.1 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-cooldown-ms 30000` - run autonomous loop with bounded retry-hint-aware breaker cooldowns so half-open probes are less likely before upstream reset windows.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25` - run autonomous loop with timeout-guarded retries plus retry-budget throttling to prevent stuck bot attempts and retry storms.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-attempt-timeout-auto --bot-attempt-timeout-auto-percentile 0.95 --bot-attempt-timeout-auto-min-samples 8 --bot-attempt-timeout-auto-window-size 32 --bot-attempt-timeout-auto-blend 0.5` - run autonomous loop with adaptive percentile timeout targeting so per-attempt deadlines track runtime drift.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-retry-max-elapsed-ms 90000 --bot-retry-budget-ratio 0.25` - run autonomous loop with per-task elapsed retry deadline capping so prolonged outage retries stop before amplifying load.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-cooldown-ms 30000` - run autonomous loop with retry hardening plus circuit-breaker fail-fast windows and cooldown probes to reduce cascading dependency failures.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-cooldown-ms 30000 --bot-circuit-breaker-half-open-max-probes 2 --bot-circuit-breaker-half-open-successes 2` - run autonomous loop with multi-probe half-open recovery gating so breakers close only after repeated probe successes.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-cooldown-ms 30000 --bot-circuit-breaker-half-open-max-probes 2 --bot-circuit-breaker-half-open-successes 2 --bot-circuit-breaker-half-open-max-wait-ms 60000` - run autonomous loop with half-open max-wait guard so stalled recovery windows reopen instead of lingering indefinitely.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-failure-rate-threshold 0.5 --bot-circuit-breaker-failure-rate-window 20 --bot-circuit-breaker-failure-rate-min-samples 8 --bot-circuit-breaker-cooldown-ms 30000` - run autonomous loop with rolling transient failure-rate circuit-breaker opening so sustained outage density triggers fail-fast protection without requiring strict failure consecutiveness.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-slow-call-rate-threshold 0.6 --bot-circuit-breaker-slow-call-duration-ms 120000 --bot-circuit-breaker-slow-call-window 20 --bot-circuit-breaker-slow-call-min-samples 8 --bot-circuit-breaker-cooldown-ms 30000` - run autonomous loop with rolling slow-call-rate circuit-breaker opening so sustained latency degradation triggers fail-fast recovery before hard failures dominate.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-cooldown-ms 30000 --bot-circuit-breaker-cooldown-backoff-multiplier 1.5 --bot-circuit-breaker-max-cooldown-ms 180000` - run autonomous loop with adaptive circuit-breaker cooldown backoff so repeated reopen events widen cooldown windows and reduce outage flapping.
- `npm run autonomous:run -- --bot-max-attempts 3 --bot-retry-base-ms 250 --bot-retry-max-ms 5000 --bot-retry-jitter 0.2 --bot-attempt-timeout-ms 120000 --bot-retry-budget-ratio 0.25 --bot-circuit-breaker-failures 4 --bot-circuit-breaker-cooldown-ms 30000 --bot-circuit-breaker-cooldown-jitter 0.2` - run autonomous loop with retry hardening plus minimum-preserving circuit-breaker cooldown jitter to reduce synchronized half-open recovery probes across workers.
- `npm run autonomous:run -- --selection-policy d_ucb_v --discount-factor 0.97 --ucb-v-exploration 1` - run autonomous loop with discounted variance-aware UCB-V adaptation for non-stationary outcomes.
- `npm run autonomous:run -- --selection-policy mw_ucb --multi-window-sizes 4,8,16,32` - run autonomous loop with multi-window UCB adaptation that opportunistically picks the strongest recency horizon under mixed drift.
- `npm run autonomous:run -- --selection-policy bob_sw_ucb --multi-window-sizes 4,8,16,32 --bob-gamma 0.12` - run autonomous loop with a bandit-over-window controller that adaptively routes SW-UCB to the strongest recency horizon while preserving exploration.
- `npm run autonomous:run -- --selection-policy ucb_tuned` - run autonomous loop with variance-aware UCB-Tuned scoring for bounded outcomes.
- `npm run autonomous:run -- --selection-policy sw_ucb_tuned --window-size 12` - run autonomous loop with sliding-window UCB-Tuned adaptation for drift-aware variance-sensitive ranking.
- `npm run autonomous:run -- --selection-policy d_ucb_tuned --discount-factor 0.97` - run autonomous loop with discounted variance-aware UCB-Tuned adaptation under drift.
- `npm run autonomous:run -- --selection-policy bayes_ucb --bayes-ucb-quantile 0.9` - run autonomous loop with Bayesian optimistic posterior quantile scoring.
- `npm run autonomous:run -- --selection-policy bayes_ucb --bayes-ucb-quantile 0.9 --thompson-meta-prior-strength 24` - run autonomous loop with empirical-Bayes prior warm-start for Bayes-UCB cold-start stabilization.
- `npm run autonomous:run -- --selection-policy sw_bayes_ucb --window-size 12 --bayes-ucb-quantile 0.9` - run autonomous loop with sliding-window Bayes-UCB adaptation to recent outcomes.
- `npm run autonomous:run -- --selection-policy d_bayes_ucb --discount-factor 0.97 --bayes-ucb-quantile 0.9` - run autonomous loop with discounted Bayes-UCB adaptation under non-stationary drift.
- `npm run autonomous:run -- --selection-policy d_epsilon_ts --discount-factor 0.97 --thompson-exploration 0.25` - run autonomous loop with discounted epsilon-Thompson adaptation for non-stationary outcomes.
- `npm run autonomous:run -- --selection-policy d_bb_ts --discount-factor 0.97 --thompson-exploration 0.25` - run autonomous loop with discounted Bayesian-bootstrap Thompson sampling under non-stationary drift.
- `npm run autonomous:run -- --selection-policy d_auto_epsilon_ts --discount-factor 0.97 --thompson-exploration 0.15 --thompson-uncertainty-weight 0.8` - run autonomous loop with discounted uncertainty-adaptive epsilon-Thompson adaptation under drift.
- `npm run autonomous:run -- --selection-policy kl_ucb --kl-ucb-confidence 3` - run autonomous loop with KL-UCB optimism for bounded binary outcomes.
- `npm run autonomous:run -- --selection-policy d_kl_ucb --discount-factor 0.97 --kl-ucb-confidence 3` - run autonomous loop with discounted KL-UCB optimism to emphasize recent bounded outcomes under drift.
- `npm run autonomous:run -- --selection-policy glr_kl_ucb --cd-min-samples 8 --cd-threshold 0.25 --cd-delta 0.02 --kl-ucb-confidence 3` - run autonomous loop with Bernoulli GLR change-detection + KL-UCB optimism for abrupt regime shifts.
- `npm run autonomous:run -- --selection-policy sw_glr_kl_ucb --window-size 12 --cd-min-samples 8 --cd-threshold 0.25 --cd-delta 0.02 --kl-ucb-confidence 3` - run autonomous loop with sliding-window Bernoulli GLR + KL-UCB adaptation focused on recent abrupt drift.
- `npm run autonomous:run -- --selection-policy cd_ucb --cd-min-samples 8 --cd-threshold 1.5 --cd-delta 0.02` - run autonomous loop with change-detection UCB for abrupt drift handling.
- `npm run autonomous:run -- --selection-policy cd_ucb --cd-min-samples 8 --cd-threshold 1.5 --cd-delta 0.02 --cd-direction down` - run autonomous loop with Page-Hinkley detection focused on downward drift only.
- `npm run autonomous:run -- --selection-policy adwin_ucb --cd-min-samples 8 --adwin-delta 0.002` - run autonomous loop with ADWIN adaptive-window UCB to auto-trim stale outcomes after detected distribution shifts.
- `npm run autonomous:run -- --selection-policy sw_cd_ucb --window-size 12 --cd-min-samples 8 --cd-threshold 1.5 --cd-delta 0.02` - run autonomous loop with sliding-window change-detection UCB to emphasize recent abrupt drift.
- `npm run autonomous:run -- --selection-policy cusum_ucb --cd-min-samples 8 --cusum-threshold 1.2 --cusum-baseline-weight 0.15` - run autonomous loop with CUSUM-based abrupt drift detection and baseline adaptation.
- `npm run autonomous:run -- --selection-policy cusum_ucb --cd-min-samples 8 --cusum-threshold 1.2 --cusum-baseline-weight 0.15 --cd-direction up` - run autonomous loop with CUSUM detection focused on upward recovery shifts only.
- `npm run autonomous:run -- --selection-policy sw_cusum_ucb --window-size 12 --cd-min-samples 8 --cusum-threshold 1.2 --cusum-baseline-weight 0.15` - run autonomous loop with sliding-window CUSUM-UCB to prioritize recent abrupt drift shifts.
- `npm run autonomous:run -- --selection-policy adwin_epsilon_ts --cd-min-samples 8 --adwin-delta 0.002 --thompson-exploration 0.25` - run autonomous loop with ADWIN adaptive-window epsilon-Thompson selection for drift-responsive posterior sampling.
- `npm run autonomous:run -- --selection-policy adwin_bb_ts --cd-min-samples 8 --adwin-delta 0.002 --thompson-exploration 0.25` - run autonomous loop with ADWIN adaptive-window Bayesian-bootstrap Thompson sampling for drift-responsive bounded-reward posterior updates.
- `npm run autonomous:run -- --selection-policy adwin_bayes_ucb --cd-min-samples 8 --adwin-delta 0.002 --bayes-ucb-quantile 0.9` - run autonomous loop with ADWIN adaptive-window Bayes-UCB optimistic posterior quantiles after detected drift.
- `npm run autonomous:run -- --selection-policy corral_exp3 --corral-gamma 0.12 --corral-eta 0.8 --corral-uncertainty-weight 0.35` - run autonomous loop with propensity-aware implicit-loss EXP3 corralling across UCB/TS/KL-UCB/change-detection experts plus optional uncertainty-aware meta-expert bonuses.
- `npm run autonomous:run -- --selection-policy sw_corral_exp3 --window-size 12 --corral-gamma 0.12 --corral-eta 0.8 --corral-uncertainty-weight 0.35` - run autonomous loop with sliding-window propensity-aware EXP3 corralling and uncertainty-aware expert exploration for recent-drift routing.
- `npm run autonomous:run -- --selection-policy d_corral_exp3 --discount-factor 0.97 --corral-gamma 0.12 --corral-eta 0.8 --corral-uncertainty-weight 0.35` - run autonomous loop with discounted propensity-aware EXP3 corralling plus uncertainty-aware expert exploration under non-stationary drift.
- `npm run autonomous:run -- --selection-policy d_corral_exp3 --discount-factor 0.97 --corral-gamma 0.12 --corral-auto-eta --corral-uncertainty-weight 0.35` - run autonomous loop with discounted corral EXP3 and effective-horizon auto-calibrated eta for lower manual tuning overhead under drift.
- `npm run autonomous:run -- --selection-policy d_corral_exp3 --discount-factor 0.97 --corral-auto-gamma --corral-auto-eta --corral-uncertainty-weight 0.35` - run autonomous loop with discounted corral EXP3 and effective-horizon auto-calibrated gamma+eta for lower manual tuning overhead under drift.
- `npm run autonomous:run -- --selection-policy adwin_corral_exp3 --cd-min-samples 8 --adwin-delta 0.002 --corral-gamma 0.12 --corral-eta 0.8 --corral-uncertainty-weight 0.35` - run autonomous loop with ADWIN-trimmed propensity-aware EXP3 corralling to drop stale expert outcomes after abrupt drift.
- `npm run autonomous:run -- --selection-policy corral_exp3_plus --corral-gamma 0.08 --corral-eta 1.2 --corral-uncertainty-weight 0.35` - run autonomous loop with expanded propensity-aware EXP3 corralling across UCB/TS/KL-UCB/Bayes-UCB/CUSUM plus ADWIN/hybrid/BGE drift-specialist experts.
- `npm run autonomous:run -- --selection-policy sw_corral_exp3_plus --window-size 12 --corral-gamma 0.08 --corral-eta 1.2 --corral-uncertainty-weight 0.35` - run autonomous loop with sliding-window expanded propensity-aware EXP3 corralling and uncertainty-aware expert routing for recency drift.
- `npm run autonomous:run -- --selection-policy d_corral_exp3_plus --discount-factor 0.97 --corral-gamma 0.08 --corral-eta 1.2 --corral-uncertainty-weight 0.35` - run autonomous loop with discounted expanded propensity-aware EXP3 corralling and uncertainty-aware expert exploration under non-stationary drift.
- `npm run autonomous:run -- --selection-policy adwin_corral_exp3_plus --cd-min-samples 8 --adwin-delta 0.002 --corral-gamma 0.08 --corral-eta 1.2 --corral-uncertainty-weight 0.35` - run autonomous loop with ADWIN-trimmed expanded propensity-aware EXP3 corralling for abrupt regime-shift expert rerouting.
- `npm run autonomous:run -- --selection-policy corral_exp3_plus --corral-gamma 0.08 --corral-eta 1.2 --corral-uncertainty-weight 0.35 --corral-min-attempts 2 --corral-forced-exploration 0.3` - run autonomous loop with explicit anti-starvation exploration mass so under-sampled experts are warmed before full exploitation.
- `npm run autonomous:run -- --selection-policy exp3_ix --exp3-ix-gamma 0.07 --exp3-ix-eta 1` - run autonomous loop with implicit-exploration EXP3 weighting for adversarial/non-stationary reward shifts.
- `npm run autonomous:run -- --selection-policy exp3_ix --exp3-ix-gamma 0.07 --exp3-auto-eta` - run autonomous loop with theorem-calibrated EXP3-IX eta and implicit gamma fallback (`eta/2`) for lower manual tuning overhead.
- `npm run autonomous:run -- --selection-policy exp3_ix --exp3-auto-gamma --exp3-auto-eta` - run autonomous loop with horizon-calibrated EXP3 explicit exploration (`gamma`) and theorem-calibrated eta for lower manual tuning overhead.
- `npm run autonomous:run -- --selection-policy exp3_ix --exp3-ix-gamma 0.07 --exp3-implicit-gamma 0.03 --exp3-ix-eta 0.8` - run autonomous loop with decoupled EXP3 exploration and implicit-exploration denominator controls.
- `npm run autonomous:run -- --selection-policy exp3_ix --exp3-ix-gamma 0.07 --exp3-ix-eta 1 --exp3-iw-cap 25` - run autonomous loop with clipped implicit importance weights to reduce extreme low-propensity variance in adversarial drift.
- `npm run autonomous:run -- --selection-policy exp3_s --exp3-ix-gamma 0.07 --exp3-ix-eta 1 --exp3-share-alpha 0.08` - run autonomous loop with EXP3.S-style share mixing to preserve exploration mass under shifting adversarial rewards.
- `npm run autonomous:run -- --selection-policy adwin_exp3_ix --cd-min-samples 8 --adwin-delta 0.002 --exp3-ix-gamma 0.07 --exp3-ix-eta 1` - run autonomous loop with ADWIN-trimmed EXP3-IX weighting to discard stale adversarial losses after detected drift.
- `npm run autonomous:run -- --selection-policy adwin_exp3_s --cd-min-samples 8 --adwin-delta 0.002 --exp3-ix-gamma 0.07 --exp3-ix-eta 1 --exp3-share-alpha 0.08` - run autonomous loop with ADWIN-trimmed EXP3.S share-mixing to preserve exploration while adapting to abrupt shifts.
- `npm run autonomous:run -- --selection-policy rexp3_ix --exp3-ix-gamma 0.07 --exp3-ix-eta 1 --exp3-restart-interval 12` - run autonomous loop with periodic EXP3-IX restarts (Rexp3-style) to reset stale adversarial weights under regime shifts.
- `npm run autonomous:run -- --selection-policy sw_exp3_ix --window-size 12 --exp3-ix-gamma 0.07 --exp3-ix-eta 1` - run autonomous loop with sliding-window EXP3-IX adaptation emphasizing recent adversarial outcomes.
- `npm run autonomous:run -- --selection-policy sw_exp3_s --window-size 12 --exp3-ix-gamma 0.07 --exp3-ix-eta 1 --exp3-share-alpha 0.08` - run autonomous loop with sliding-window EXP3.S-style share mixing for recency-aware adversarial drift.
- `npm run autonomous:run -- --selection-policy d_exp3_ix --discount-factor 0.97 --exp3-ix-gamma 0.07 --exp3-ix-eta 1` - run autonomous loop with discounted EXP3-IX adaptation to prioritize recent non-stationary reward shifts.
- `npm run autonomous:run -- --selection-policy d_exp3_s --discount-factor 0.97 --exp3-ix-gamma 0.07 --exp3-ix-eta 1 --exp3-share-alpha 0.08` - run autonomous loop with discounted EXP3.S-style share mixing for non-stationary adversarial adaptation.
- `npm run autonomous:run -- --selection-policy tsallis_inf --exp3-ix-gamma 0.07 --tsallis-eta-scale 1` - run autonomous loop with Tsallis-INF adversarial weighting using propensity-aware implicit losses.
- `npm run autonomous:run -- --selection-policy tsallis_inf --exp3-ix-gamma 0.07 --tsallis-eta-scale 1 --tsallis-auto-eta` - run autonomous loop with Tsallis-INF auto-calibrated eta schedule (`4*scale/sqrt(N)`) for lower manual tuning overhead.
- `npm run autonomous:run -- --selection-policy tsallis_inf --exp3-auto-gamma --tsallis-eta-scale 1 --tsallis-auto-eta` - run autonomous loop with horizon-calibrated Tsallis-INF exploration mixing plus auto-eta tuning.
- `npm run autonomous:run -- --selection-policy sw_tsallis_inf --window-size 12 --exp3-ix-gamma 0.07 --tsallis-eta-scale 1` - run autonomous loop with sliding-window Tsallis-INF adaptation emphasizing recent adversarial outcomes.
- `npm run autonomous:run -- --selection-policy adwin_tsallis_inf --cd-min-samples 8 --adwin-delta 0.002 --exp3-ix-gamma 0.07 --tsallis-eta-scale 1` - run autonomous loop with ADWIN-trimmed Tsallis-INF adaptation to drop stale adversarial outcomes after abrupt drift.
- `npm run autonomous:run -- --selection-policy d_tsallis_inf --discount-factor 0.97 --exp3-ix-gamma 0.07 --tsallis-eta-scale 1` - run autonomous loop with discounted Tsallis-INF adaptation to prioritize recent non-stationary adversarial rewards.
- `npm run autonomous:run -- --selection-policy bge --boltzmann-gumbel-c 0.5` - run autonomous loop with Boltzmann-Gumbel exploration using uncertainty-scaled perturbations.
- `npm run autonomous:run -- --selection-policy sw_bge --window-size 12 --boltzmann-gumbel-c 0.5` - run autonomous loop with sliding-window Boltzmann-Gumbel adaptation for recent drift.
- `npm run autonomous:run -- --selection-policy d_bge --discount-factor 0.97 --boltzmann-gumbel-c 0.5` - run autonomous loop with discounted Boltzmann-Gumbel exploration for non-stationary regimes.
- `npm run autonomous:run -- --selection-policy phe --phe-perturbation-scale 2` - run autonomous loop with Perturbed-History Exploration pseudo-reward perturbations for efficient stochastic exploration.
- `npm run autonomous:run -- --selection-policy sw_phe --window-size 12 --phe-perturbation-scale 2` - run autonomous loop with sliding-window Perturbed-History Exploration for recency-aware drift adaptation.
- `npm run autonomous:run -- --selection-policy d_phe --discount-factor 0.97 --phe-perturbation-scale 2` - run autonomous loop with discounted Perturbed-History Exploration to emphasize recent outcomes under drift.
- `npm run autonomous:run -- --selection-policy moss_anytime --moss-alpha 1.2` - run autonomous loop with anytime-MOSS minimax exploration for hard cold-start allocation.
- `npm run autonomous:run -- --selection-policy sw_moss_anytime --window-size 12 --moss-alpha 1.2` - run autonomous loop with sliding-window anytime-MOSS adaptation focused on recent outcomes under drift.
- `npm run autonomous:run -- --selection-policy d_moss_anytime --discount-factor 0.97 --moss-alpha 1.2` - run autonomous loop with discounted anytime-MOSS adaptation to emphasize recency in non-stationary conditions.
- `npm run analyze` - generate cognition analysis report.
- `npm run analyze:quick` - run a 1-day fast analysis pass.
- `tsx scripts/analyze-history.ts --days 7 --json reports/cognition-report.json --markdown reports/cognition-report.md` - produce historical analysis report outputs.
- `npm run graph` - render cognition graph outputs from current analysis state.
- `npm run plan:tasks` - emit remediation task requests.
- `npm run enqueue:tasks` - enqueue generated task bundles for execution.
- `npm run dispatch:created` - dispatch newly created queued tasks.
- `npm run learn:loop` - run learning-loop replay outputs.
- `npm run plan:skills` - emit skill-growth task planning outputs.
- `npm run review:approvals` - process approval-review automation for queued work.
- `npm run process:outbox` - process outbox task execution and retries.
- `npm run autopilot:loop` - run the autonomous task processing loop.
- `npm run build:full` - run the full cognition build pipeline.
- `npm run memory:guardrails` - generate memory guardrail reports.
- `npm run memory:backfill` - backfill memory guardrail reports.
- `npm run iterate:plan` - generate cognition iteration plan + task bundle artifacts.
- `npm run typecheck` - run TypeScript no-emit type checks.
- `npm test` - run swarm-protocol tests (unit + legacy).
- `npm run demo:orchestrator` - run orchestrator demo flow.
- `npm run approval:queue` - export approval queue reports.
- `npm run benchmark:simulate` - run deterministic simulation benchmarks.
- `npm run ops -- status` - inspect operator status.
- `npm run ops -- queue --limit 10` - inspect queued tasks with a bounded view.
- `npm run ops -- tail --limit 20` - tail recent lifecycle events.
- `tsx scripts/auto-refactor.ts` - run repo self-lint checks (syntax, script entrypoints, relative imports).

## TODO
- Verified `4c27058adc..6cf42ba4a5`: no new README command-map additions were found (range adds warfighter wave LVI gap-closure skill content only); `package.json` script-map diff remains partially blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `fdf41e71b4..4c27058adc`: added README command-map workflow for delayed-feedback exponential decay (`--selection-policy d_ucb`, `--discount-factor`, `--feedback-delay-decay-ms`) and aligned autonomous runtime/CLI plumbing plus regression coverage so stale outcomes are down-weighted instead of fully discarded; verified workflow command list already contains this command; `package.json` script-map diff remains partially blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `6d34b374b0..cb5f793af2`: no new README command-map additions were found (range adds warfighter wave LV escalation support-cell skill content only); `package.json` script-map diff remains partially blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `39846fd701..6d34b374b0`: added README command-map workflow for delayed-feedback gating (`--selection-policy d_ucb`, `--discount-factor`, `--max-feedback-delay-ms`) and aligned outbox + worker-loop + autonomous runtime/CLI plumbing plus regression coverage so excessively delayed outcomes are skipped from ranking updates; verified workflow command list now includes this command; `package.json` script-map diff remains partially blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c079b12420..0633bda8b2`: no new README command-map additions were found (range adds warfighter wave LIV exposure/fatigue skill content only); `package.json` script-map diff remains partially blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `1552563610..fa286846f2`: no new README command-map additions were found (range adds delayed-feedback-aware autonomous selection handling in runtime/tests only); `package.json` script-map diff remains partially blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5fbbf8b17a..5fe224b9ea`: no new README command-map additions were found (range adds warfighter waves LII-LIII continuity/occupational-health skill content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5b8d26381b..5270537cee`: no new README command-map additions were found (range adds warfighter replay expansion wave content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `f686aa24ce..5b8d26381b`: added README command-map workflow for timeout-guarded transient-failure retries with elapsed retry-deadline capping (`--bot-max-attempts`, `--bot-retry-base-ms`, `--bot-retry-max-ms`, `--bot-retry-jitter`, `--bot-retry-max-elapsed-ms`, `--bot-retry-budget-ratio`) and aligned outbox + worker-loop + autonomous runtime/CLI plumbing plus regression coverage for bounded per-task retry lifetimes under prolonged upstream degradation; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `7ef77e7ac8..c0b0bcbbae`: added README command-map workflow for retry-hint-jitter plus circuit-breaker cooldown stacking (`--bot-max-attempts`, `--bot-retry-base-ms`, `--bot-retry-max-ms`, `--bot-retry-jitter`, `--bot-retry-hint-max-ms`, `--bot-retry-hint-jitter`, `--bot-circuit-breaker-failures`, `--bot-circuit-breaker-cooldown-ms`), and improved runtime behavior so bounded retry hints also inform breaker reopen cooldown timing; verified workflow command list already contained this command, so no verified-workflow entry changes were required; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifest is absent.
- Verified `96296f8f6e..d450fb7ef2`: no new README command-map additions were found (range adds warfighter wave LVIII mission-cell skill content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `a58469df0b..96296f8f6e`: added README command-map workflow for half-open max-wait guardrails (`--bot-circuit-breaker-half-open-max-wait-ms`) stacked with existing retry-budget/circuit-breaker probe controls, aligned outbox + worker-loop + autonomous runtime/CLI plumbing, and added regression coverage so stalled half-open windows reopen instead of lingering indefinitely; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `f4006e0fbc..a58469df0b`: added README command-map workflow for minimum-preserving circuit-breaker cooldown jitter (`--bot-circuit-breaker-cooldown-jitter`) stacked with existing retry-budget/fail-fast controls, aligned outbox + worker-loop + autonomous runtime/CLI cooldown scheduling, and added regression coverage for jittered breaker reopen windows; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `250ab945f5..f4006e0fbc`: no new README command-map additions were found (range adds warfighter family-support/prosthetic-surge skill content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `81adfb46f3..250ab945f5`: no new README command-map additions were found (range tightens retry-hint-jitter semantics to preserve minimum server wait time and broadens Retry-After/RateLimit-Reset hint parsing coverage without introducing new command flags); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `ddc9c87ac0..81adfb46f3`: no new README command-map additions were found (range adds warfighter wave LVI support-skill content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `9f0bc248a8..ddc9c87ac0`: added README command-map workflow for bounded Retry-After/RateLimit-Reset honoring plus jittered hint delays (`--bot-max-attempts`, `--bot-retry-base-ms`, `--bot-retry-max-ms`, `--bot-retry-jitter`, `--bot-retry-hint-max-ms`, `--bot-retry-hint-jitter`), aligned outbox + worker-loop + autonomous runtime/CLI hint parsing and delay jitter plumbing, and added regression coverage for jittered server hint delays with `RateLimit-Reset`/absolute-date fallback parsing; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `e8788050e7..04add235e7`: added README command-map workflow for bounded Retry-After hint honoring stacked with transient-failure retry jitter controls (`--bot-max-attempts`, `--bot-retry-base-ms`, `--bot-retry-max-ms`, `--bot-retry-jitter`, `--bot-retry-hint-max-ms`), aligned outbox + worker-loop + autonomous runtime/CLI hint-delay plumbing, and added regression coverage for honoring parsed retry hints without exceeding configured delay caps; no additional README command-map additions were found in the intervening AGENTS verification bookkeeping commit; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `a76e387ec4..e8788050e7`: no new README command-map additions were found (range adds warfighter wave LV support-skill content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `1468e754ca..a76e387ec4`: added README command-map workflow for transient-failure retry jitter strategy controls (`--bot-max-attempts`, `--bot-retry-base-ms`, `--bot-retry-max-ms`, `--bot-retry-jitter`, `--bot-retry-jitter-strategy decorrelated`) and expanded runtime guidance for `--bot-retry-jitter-strategy symmetric|full|decorrelated`; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `7e5af1a0fb..c5994f0ae2`: no new README command-map additions were found (range adds warfighter expansion wave LIV rescue/trust-cell content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `f96af53c2f..7e5af1a0fb`: added README command-map workflow for top-two epsilon-Thompson autonomous run routing (`--selection-policy tt_epsilon_ts`, `--thompson-exploration`, `--thompson-top-two-probability`, `--thompson-prior-alpha`, `--thompson-prior-beta`) and aligned autonomous runtime/CLI support plus regression coverage for leader/challenger top-two Thompson exploration behavior; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `96d23f29af..5c06ff059d`: added README command-map workflow for rolling slow-call-rate circuit-breaker opening (`--bot-circuit-breaker-slow-call-rate-threshold`, `--bot-circuit-breaker-slow-call-duration-ms`, `--bot-circuit-breaker-slow-call-window`, `--bot-circuit-breaker-slow-call-min-samples`) stacked with existing retry/budget/cooldown controls, aligned outbox + worker-loop + autonomous runtime/CLI plumbing, and added regression coverage for slow-call-rate-triggered breaker opens plus minimum-sample gating; no additional README command-map additions were found in the intervening warfighter expansion wave LIII commit; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `4bc429c4be..96d23f29af`: added README command-map workflow for adaptive circuit-breaker cooldown backoff (`--bot-circuit-breaker-cooldown-backoff-multiplier`, `--bot-circuit-breaker-max-cooldown-ms`) stacked with existing retry/budget/fail-fast controls, aligned outbox + worker-loop + autonomous runtime/CLI plumbing, and added regression coverage for escalating cooldown windows across repeated reopen events; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `2001803032..4bc429c4be`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5320e18dd2..2001803032`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `3575e11015..5320e18dd2`: added README command-map workflow for rolling transient failure-rate circuit-breaker guardrails (`--bot-circuit-breaker-failure-rate-threshold`, `--bot-circuit-breaker-failure-rate-window`, `--bot-circuit-breaker-failure-rate-min-samples`) stacked with existing retry/cooldown controls, aligned outbox + worker-loop + autonomous runtime/CLI plumbing, and added regression coverage for failure-rate-triggered breaker opens plus minimum-sample gating; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `9f47202b6a..a97e94c5d8`: no new README command-map additions were found (range adds AGENTS verification bookkeeping plus warfighter expansion wave LII support-cell content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `63cef8b9fe..9f47202b6a`: added README command-map workflow for multi-probe half-open circuit-breaker recovery gating (`--bot-circuit-breaker-half-open-max-probes`, `--bot-circuit-breaker-half-open-successes`), aligned outbox + worker-loop + autonomous runtime/CLI telemetry plumbing, and added regression coverage for requiring repeated half-open probe successes before breaker closure; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `e36c9088ba..732f0f93b7`: added README command-map workflow for autonomous bot circuit-breaker fail-fast windows (`--bot-circuit-breaker-failures`, `--bot-circuit-breaker-cooldown-ms`) stacked with timeout-guarded retry-budget controls, aligned runtime/CLI plumbing across outbox + worker-loop + autonomous entrypoints, and added regression coverage for circuit-open fail-fast plus half-open recovery behavior; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `2d14e529cc..68222185e8`: added README command-map workflow for timeout-guarded autonomous bot retries with retry-budget throttling (`--bot-max-attempts`, `--bot-retry-base-ms`, `--bot-retry-max-ms`, `--bot-retry-jitter`, `--bot-attempt-timeout-ms`, `--bot-retry-budget-ratio`) and aligned autonomous runtime + regression coverage for per-attempt timeout cancellation and bounded retry amplification under transient degradation; no additional README command-map additions were found in the intervening AGENTS verification bookkeeping and warfighter expansion wave LI commits; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `31e76121d1..2d14e529cc`: added README command-map workflow for autonomous bot transient-failure retry controls (`--bot-max-attempts`, `--bot-retry-base-ms`, `--bot-retry-max-ms`, `--bot-retry-jitter`) and aligned autonomous runtime + regression coverage for exponential backoff with jitter on retryable bot faults; no additional README command-map additions were found in the intervening AGENTS verification bookkeeping commit; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `0be9e79069..31e76121d1`: added README command-map workflow for discounted UCB CVaR tail-latency ranking (`--selection-policy d_ucb`, `--discount-factor`, `--latency-target-ms`, `--latency-cvar-penalty-weight`, `--latency-cvar-percentile`, `--latency-cvar-min-samples`) and aligned autonomous runtime + regression coverage for CVaR tail-overrun penalties; no additional README command-map additions were found in the intervening AGENTS verification bookkeeping commit; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5a31d86ca5..0be9e79069`: added README command-map workflow for discounted UCB latency-SLA miss-burst ranking guardrail (`--selection-policy d_ucb`, `--discount-factor`, `--latency-sla-ms`, `--latency-burst-penalty-weight`, `--latency-burst-short-window`, `--latency-burst-long-window`, `--latency-burst-min-attempts`, `--latency-burst-threshold`) and aligned autonomous runtime + regression coverage for short-vs-long latency-SLA miss-rate burst penalties; no additional README command-map additions were found in the intervening AGENTS verification bookkeeping and warfighter expansion wave L commits; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `7e33ec36c5..5a31d86ca5`: added README command-map workflow for discounted UCB failure-burst ranking guardrail (`--selection-policy d_ucb`, `--discount-factor`, `--failure-burst-penalty-weight`, `--failure-burst-short-window`, `--failure-burst-long-window`, `--failure-burst-min-attempts`, `--failure-burst-threshold`) and aligned autonomous runtime + regression coverage for abrupt-regression failure-burst penalties; no additional README command-map additions were found in the intervening AGENTS verification bookkeeping and warfighter expansion wave XLIX commits; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `14b4673bd0..7e33ec36c5`: added README command-map workflow for discounted UCB percentile tail-latency ranking (`--selection-policy d_ucb`, `--discount-factor`, `--latency-target-ms`, `--latency-tail-penalty-weight`, `--latency-tail-percentile`, `--latency-tail-min-samples`) and aligned autonomous runtime + regression coverage for percentile overrun penalties; no additional README command-map additions were found in the intervening AGENTS verification bookkeeping and warfighter expansion wave XLVIII commits; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b6a0906731..14b4673bd0`: added README command-map workflow for discounted UCB conservative latency-SLA deadline-hit ranking (`--selection-policy d_ucb`, `--discount-factor`, `--latency-sla-ms`, `--latency-sla-floor`, `--latency-sla-min-attempts`) and aligned autonomous runtime + regression coverage for Wilson-confidence SLA-hit penalties; no additional README command-map additions were found in the intervening AGENTS bookkeeping and warfighter expansion wave XLVII commits; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `da341f34c6..b6a0906731`: added README command-map workflow for discounted UCB latency-aware shaping with recency-windowed adaptive targets and blended stabilization (`--selection-policy d_ucb`, `--discount-factor`, `--latency-penalty-weight`, `--latency-target-ms`, `--latency-auto-target`, `--latency-auto-target-percentile`, `--latency-auto-target-min-samples`, `--latency-auto-target-window-size`, `--latency-auto-target-blend`) and aligned autonomous runtime + regression coverage for windowed/blended latency-target reward shaping; no additional README command-map additions were found in the intervening AGENTS bookkeeping commit; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `8ad630a72b..da341f34c6`: added README command-map workflow for discounted UCB conservative reliability-floor ranking (`--selection-policy d_ucb`, `--discount-factor`, `--reliability-floor`, `--reliability-floor-min-attempts`) and aligned autonomous runtime + regression coverage for Wilson-confidence reliability penalties; no additional README command-map additions were found in the follow-on AGENTS bookkeeping commit; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `3fc145b8be..8ad630a72b`: added README command-map workflow for discounted UCB latency-aware shaping with percentile-adaptive auto-target controls (`--selection-policy d_ucb`, `--discount-factor`, `--latency-penalty-weight`, `--latency-target-ms`, `--latency-auto-target`, `--latency-auto-target-percentile`, `--latency-auto-target-min-samples`) and aligned autonomous runtime + regression coverage for adaptive latency-target reward shaping; no additional README command-map additions were found in the follow-on warfighter expansion wave XLVI commit; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c57bc2da88..d0c7d4f3c5`: no new README command-map additions were found (range adds AGENTS verification bookkeeping plus warfighter expansion waves XLIII-XLV skill-set content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `cc1a22d9e6..c57bc2da88`: added README command-map workflow for corral EXP3 effective-horizon auto-gamma + auto-eta calibration (`--selection-policy d_corral_exp3`, `--discount-factor`, `--corral-auto-gamma`, `--corral-auto-eta`, `--corral-uncertainty-weight`) and aligned autonomous runtime + regression coverage so corral implicit-loss scoring and exploration mixing both use calibrated horizon-aware gamma; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b7e4b18d66..20124784f2`: added README command-map workflow for discounted UCB latency-aware reward shaping (`--selection-policy d_ucb`, `--discount-factor`, `--latency-penalty-weight`, `--latency-target-ms`) and aligned autonomous runtime + regression coverage for latency-penalized scoring; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `f5fce1f8eb..7c2fb2c68a`: added README command-map workflows for horizon-calibrated adversarial auto-gamma runs (`--selection-policy exp3_ix|tsallis_inf`, `--exp3-auto-gamma`, `--exp3-auto-eta`, `--tsallis-eta-scale`, `--tsallis-auto-eta`) and aligned autonomous runtime + regression coverage for effective-horizon gamma calibration; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `1404b69d59..d6b2dfd1a9`: no new README command-map additions were found (range updates EXP3-IX implicit exploration defaults in autonomy runtime/tests only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `2465819dfe..43674b0723`: no new README command-map additions were found (range adds AGENTS verification bookkeeping plus warfighter expansion wave XLII skill-set content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `36b6102333..0af00cfa99`: no new README command-map additions were found (range adds AGENTS verification bookkeeping/hash-range correction plus warfighter expansion wave XLI continuity/trust-cell skill-set content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `0af00cfa99..2465819dfe`: added README command-map workflow for corral EXP3 effective-horizon auto-eta calibration (`--selection-policy d_corral_exp3`, `--discount-factor`, `--corral-gamma`, `--corral-auto-eta`, `--corral-uncertainty-weight`) and aligned autonomous runtime + regression coverage for discounted recency-aware corral eta tuning; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `8f2fa94212..135a6df4bd`: no new README command-map additions were found (range adds warfighter expansion wave XL operational support-cell skill-set content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `135a6df4bd9a..36b6102333`: no new README command-map workflow additions were required; upgraded autonomous selection cooldown gating to use deterministic exponential backoff by consecutive failure streak (with bounded jitter) so repeatedly failing skills/capabilities are retried less aggressively while preserving eventual re-entry, and added regression coverage for deep-streak cooldown behavior; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b9747e023b..30d1eda9d8`: no new README command-map workflow additions were required; hardened adversarial `exp3_ix*`/`tsallis_inf*` auto-eta calibration to use active recency horizon evidence (including discounted effective sample size for `d_*` modes), removed duplicated discounted-mode branching via shared adversarial mode sets, and added regression coverage for discounted auto-eta horizon behavior; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `45c5f6797f..b9747e023b`: no new README command-map additions were found (range adds warfighter expansion wave XXXIX continuity-cell skill-set content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `6607f0aba0..45c5f6797f`: added README command-map workflow for Tsallis-INF auto-eta calibration (`--selection-policy tsallis_inf`, `--exp3-ix-gamma`, `--tsallis-eta-scale`, `--tsallis-auto-eta`) and aligned autonomous runtime + regression coverage for implicit-gamma-smoothed Tsallis-INF scoring; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b792b15d47..f0760041e0`: no new README command-map additions were found (range adds AGENTS verification bookkeeping plus warfighter expansion wave XXXIII-XXXVII skill-set content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `f0760041e0..6607f0aba0`: no new README command-map workflow additions were required; upgraded `tsallis_inf*` scoring to a propensity-aware reduced-variance estimator with baseline gating (`eta^2`) plus discounted-mode compatibility, and added regression coverage for reduced-variance ranking behavior; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `399219ca32..b792b15d47`: added README command-map workflow for ADWIN-trimmed Tsallis-INF autonomous runs (`--selection-policy adwin_tsallis_inf`, `--cd-min-samples`, `--adwin-delta`, `--exp3-ix-gamma`, `--tsallis-eta-scale`) and wired autonomous runtime + regression coverage for abrupt-drift Tsallis-entropy implicit-loss reranking; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `658b0b3c9c..399219ca32`: added README command-map workflows for Tsallis-INF adversarial selectors (`--selection-policy tsallis_inf|sw_tsallis_inf|d_tsallis_inf`, `--window-size`, `--discount-factor`, `--exp3-ix-gamma`, `--tsallis-eta-scale`) and wired autonomous runtime + regression coverage for Tsallis-entropy propensity-aware implicit-loss ranking under stationary and drifted outcomes; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `3d28dd97d7..c77c014a59`: added README command-map workflow for EXP3-IX implicit-importance clipping controls (`--selection-policy exp3_ix`, `--exp3-ix-gamma`, `--exp3-ix-eta`, `--exp3-iw-cap`) and aligned autonomous runtime + regression coverage for capped implicit-loss variance under low-propensity outcomes; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c77c014a59..b3aae72294`: added README command-map workflows for ADWIN-trimmed corral EXP3 variants (`--selection-policy adwin_corral_exp3|adwin_corral_exp3_plus`, `--cd-min-samples`, `--adwin-delta`, `--corral-gamma`, `--corral-eta`, `--corral-uncertainty-weight`) and extended autonomous runtime + regression coverage for abrupt-drift expert corralling; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5faf32d792..fdc1b0b4b8`: no new README command-map additions were found (range adds AGENTS verification bookkeeping/hash correction plus warfighter skill/reference content and docs timestamp updates only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `fdc1b0b4b8..3d28dd97d7`: no new README command-map workflow additions were required; upgraded `corral_exp3*` routing to propensity-aware implicit-loss EXP3 weighting (with sliding-window/discounted recency semantics), persisted `selectionPolicyProbability` into autonomy task context for policy-level propensity telemetry, and added regression coverage for corral propensity-aware expert selection plus coverage ingestion fallback precedence; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `21537fc2ba..5faf32d792`: added README command-map workflows for ADWIN-trimmed EXP3 variants (`--selection-policy adwin_exp3_ix|adwin_exp3_s`, `--cd-min-samples`, `--adwin-delta`, `--exp3-ix-gamma`, `--exp3-ix-eta`, `--exp3-share-alpha`) and extended autonomous runtime + regression coverage for adaptive-window adversarial weighting; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `a96aeb0c5e..21537fc2ba`: no new README command-map additions were found (range adds AGENTS verification bookkeeping/hash correction only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c62bc52072..a705e29f3c`: no new README command-map additions were found (range adds AGENTS verification bookkeeping/hash correction only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `a705e29f3c..a96aeb0c5e`: no new README command-map workflow additions were required; aligned EXP3-IX runtime with propensity-aware implicit-loss estimation from autonomy outcome history (including sliding-window/discounted/restart variants), persisted `selectionProbability` into recent outcome telemetry for replayed coverage, and added regression coverage + README policy guidance for the new EXP3 estimator behavior; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5135dec6c6..c62bc52072`: added README command-map workflows for empirical-Bayes Bayesian prior warm-start controls (`--thompson-meta-prior-strength`) across `epsilon_ts` and `bayes_ucb`, and wired autonomous runtime + regression coverage for global-pseudocount cold-start stabilization; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `982302987b..5135dec6c6`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `a4910f5567..982302987b`: added README command-map workflows for ADWIN Bayesian selectors (`--selection-policy adwin_bb_ts|adwin_bayes_ucb`, `--cd-min-samples`, `--adwin-delta`, `--thompson-exploration`, `--bayes-ucb-quantile`) and extended autonomous runtime policy support plus regression coverage for adaptive-window reranking; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c4737308ac..caa1dcf14c`: added README command-map workflow for BOB-style adaptive window routing (`--selection-policy bob_sw_ucb`, `--multi-window-sizes`, `--bob-gamma`) and wired autonomous runtime + coverage tracking for selected-window meta-policy outcomes; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b895d565e2..f82989e9e6`: no new README command-map additions were found (range adds AGENTS verification bookkeeping plus warfighter skill/reference content and bindings only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `204af43363..b895d565e2`: added README command-map workflows for ADWIN-contextual autonomous run flags (`--selection-policy adwin_linucb|adwin_lints`, `--adwin-delta`, `--cd-min-samples`, `--linucb-alpha`, `--lints-alpha`) and expanded autonomous policy telemetry tracking across all supported selectors; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b4b7b23ab1..e43ede7cdb`: no new README command-map additions were found (range adds bot idempotent task replay cache/collision-guard behavior only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `0692189a76..b4b7b23ab1`: added README command-map workflow for anti-starvation corral expert exploration controls (`--selection-policy corral_exp3_plus`, `--corral-gamma`, `--corral-eta`, `--corral-min-attempts`, `--corral-forced-exploration`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `072dab6f97..0692189a76`: added README command-map workflows for Perturbed-History Exploration autonomous run flags (`--selection-policy phe|sw_phe|d_phe`, `--window-size`, `--discount-factor`, `--phe-perturbation-scale`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5a44065c43..072dab6f97`: no new README command-map additions were found (range adds AGENTS verification bookkeeping plus warfighter skill/reference content and bindings only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `072dab6f97..2f9807132f`: added README command-map workflows and CLI/runtime support for uncertainty-aware corral expert routing (`--corral-uncertainty-weight`) across `corral_exp3*` variants, plus regression coverage for under-sampled expert exploration in `corral_exp3`; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `0692189a76..bb0fd19715`: added README command-map workflows for GLR + KL-UCB abrupt-drift autonomous run flags (`--selection-policy glr_kl_ucb|sw_glr_kl_ucb`, `--window-size`, `--cd-min-samples`, `--cd-threshold`, `--cd-delta`, `--kl-ucb-confidence`) and aligned autonomous CLI help text for `cd_*`/`glr_*` flags; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `072dab6f97..90e79986be`: added README command-map workflows for mean-variance UCB autonomous run flags (`--selection-policy mv_ucb|sw_mv_ucb|d_mv_ucb`, `--window-size`, `--discount-factor`, `--risk-variance-weight`) and aligned CLI help/validation plus autonomy scoring for risk-aware volatility penalties; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `59ca2b49f6..5a44065c43`: added README command-map workflows for adaptive hybrid `fdsw_*` aggregation (`--selection-policy fdsw_epsilon_ts|fdsw_ucb`, `--window-size`, `--discount-factor`, `--hybrid-ts-aggregation adaptive`, `--thompson-exploration`) and aligned CLI help/validation to accept `adaptive`; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5f681993ae..5b4ba94156`: added README command-map workflows for EXP3.S-style share-mixing autonomous run flags (`--selection-policy exp3_s|sw_exp3_s|d_exp3_s`, `--window-size`, `--discount-factor`, `--exp3-ix-gamma`, `--exp3-ix-eta`, `--exp3-share-alpha`); `package.json` script-map diff remains blocked in this checkout because root package manifest is absent.
- Verified `b95647a4cf..555b6e877f`: no new README command-map additions were found (range adds warfighter skill/reference content and protocol/tool addenda only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `6bff313649..1f01921ab0`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `1f01921ab0..9b7e8174c5`: expanded README command-map guidance for `corral_exp3_plus` to include ADWIN/hybrid/BGE drift-specialist experts; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `1f15012e58..6bff313649`: added README command-map workflow for hybrid discounted+sliding-window UCB autonomous run flags (`--selection-policy fdsw_ucb`, `--window-size`, `--discount-factor`, `--hybrid-ts-aggregation`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `d6321e149a..0141c23215`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c0ab726335..d6321e149a`: added README command-map workflows for ADWIN adaptive-window autonomous run flags (`--selection-policy adwin_ucb|adwin_epsilon_ts`, `--adwin-delta`, `--cd-min-samples`, `--thompson-exploration`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `fa8dbb3f79..4dfe4479ea`: added README command-map workflows for Boltzmann-Gumbel autonomous run flags (`--selection-policy bge|sw_bge|d_bge`, `--window-size`, `--discount-factor`, `--boltzmann-gumbel-c`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `6f35422c90..1c7fa01e9d`: no new README command-map additions were found (range adds warfighter skill/reference content and protocol bindings only); `package.json` script-map diff remains blocked in this checkout because `swarm-protocol` package manifest is absent.
- Verified `a748d29c22..6f35422c90`: added README command-map workflows for Bayesian-bootstrap Thompson autonomous run flags (`--selection-policy bb_ts|sw_bb_ts|d_bb_ts`, `--window-size`, `--discount-factor`, `--thompson-exploration`); `package.json` script-map diff remains blocked in this checkout because `swarm-protocol` package manifest is absent.
- Verified `1c6bbf5115..96c37731fa`: no new README command-map additions were found (range updates AGENTS verification bookkeeping/hash correction only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `063c6cc351..1c6bbf5115`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c5c9169414..cee4aa726a`: no new README command-map additions were found (range adds warfighter skill/reference content and protocol override updates only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `cee4aa726a..063c6cc351`: added README command-map workflow for multi-window UCB autonomous run flags (`--selection-policy mw_ucb`, `--multi-window-sizes`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `db43beb77f..3086a52544`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `3086a52544..9e4b0af1a2`: added README command-map workflow for periodic-restart EXP3-IX autonomous run flags (`--selection-policy rexp3_ix`, `--exp3-ix-gamma`, `--exp3-ix-eta`, `--exp3-restart-interval`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `9e4b0af1a2..d34ea7478a`: added README command-map workflows for recency-aware policy corralling variants (`--selection-policy sw_corral_exp3|d_corral_exp3|sw_corral_exp3_plus|d_corral_exp3_plus`, `--window-size`, `--discount-factor`, `--corral-gamma`, `--corral-eta`); `package.json` script-map diff remains blocked in this checkout because root package manifest is absent.
- Verified `8dca4f38ab..db43beb77f`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `28fdda7164..8dca4f38ab`: added README command-map workflows for surprise-adaptive changepoint epsilon-Thompson autonomous run flags (`--selection-policy cp_epsilon_ts|sw_cp_epsilon_ts`, `--window-size`, `--thompson-hazard-rate`, `--thompson-surprise-sensitivity`, `--thompson-exploration`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `304006cfcd..aa27f3ed5c`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `2165a0c469..304006cfcd`: added README command-map workflows for directional change-detection autonomous run flags (`--cd-direction up|down|both`) across Page-Hinkley/CUSUM examples; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `8eaff02804..2165a0c469`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `aa27f3ed5c..61bfb8a9fc`: added README command-map workflows for surprise-adaptive changepoint epsilon-Thompson autonomous run flags (`--selection-policy cp_epsilon_ts|sw_cp_epsilon_ts`, `--window-size`, `--thompson-hazard-rate`, `--thompson-surprise-sensitivity`, `--thompson-exploration`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `ac6793d4ea..8eaff02804`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `8eaff02804..b37400f851`: added README command-map workflows for directional change-detection autonomous run flags (`--cd-direction up|down|both`) across Page-Hinkley/CUSUM examples; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `956c8340fd..5053f859b3`: added README command-map workflows for change-detection epsilon-Thompson autonomous run flags (`--selection-policy cd_epsilon_ts|sw_cd_epsilon_ts|cusum_epsilon_ts|sw_cusum_epsilon_ts`, `--window-size`, `--cd-min-samples`, `--cd-threshold`, `--cd-delta`, `--cusum-threshold`, `--cusum-baseline-weight`, `--thompson-exploration`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `71f3717dae..ac6793d4ea`: added README command-map workflows for uncertainty-adaptive epsilon-Thompson autonomous run flags (`--selection-policy auto_epsilon_ts|sw_auto_epsilon_ts|d_auto_epsilon_ts`, `--window-size`, `--discount-factor`, `--thompson-exploration`, `--thompson-uncertainty-weight`, `--thompson-prior-alpha`, `--thompson-prior-beta`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `8889db4d1b..956c8340fd`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `3893284622..c520cf64c9`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `fa307c8e93..8889db4d1b`: added README command-map workflow for hybrid discounted+sliding-window epsilon-Thompson autonomous run flags (`--selection-policy fdsw_epsilon_ts`, `--window-size`, `--discount-factor`, `--hybrid-ts-aggregation`, `--thompson-exploration`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `282ce3cb9a..3893284622`: added README command-map workflows for sliding-window change-detection autonomous run flags (`--selection-policy sw_cd_ucb|sw_cusum_ucb`, `--window-size`, `--cd-min-samples`, `--cd-threshold`, `--cd-delta`, `--cusum-threshold`, `--cusum-baseline-weight`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b4d4521cd8..84c8746dd5`: added README command-map workflows for drift-aware anytime-MOSS autonomous run flags (`--selection-policy sw_moss_anytime|d_moss_anytime`, `--window-size`, `--discount-factor`, `--moss-alpha`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `98891794db..b4d4521cd8`: no new README command-map additions were found (range adds AGENTS verification bookkeeping/follow-up range correction only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `09c126ded9..1d3941f8d9`: no new README command-map additions were found (range adds AGENTS verification bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `ac20ccf744..09c126ded9`: added README command-map workflow for discounted KL-UCB autonomous run flags (`--selection-policy d_kl_ucb`, `--discount-factor`, `--kl-ucb-confidence`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `147359bd7b..22aee8f278`: `package.json` script-map diff is blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent; re-run script verification once manifests are present.
- Verified `22aee8f278..c4c4e79215`: `package.json` script-map diff is blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent; re-run script verification once manifests are present.
- Verified `c4c4e79215..19a0116019`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `19a0116019..df28e28128`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `df28e28128..79f17aa696`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `79f17aa696..e4eab1e1c7`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `e4eab1e1c7..2b0c58c136`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `2b0c58c136..8fa958cd51`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `8fa958cd51..fe989d6dac`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `fe989d6dac..405354a5e0`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `405354a5e0..9d028c8fa0`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `9d028c8fa0..eabf0e7826`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `eabf0e7826..dcd2a7f3f7`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `dcd2a7f3f7..f0aaa02329`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `f0aaa02329..3dd6aeed23`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `3dd6aeed23..e906187f39`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `e906187f39..f12bed52f4`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `f12bed52f4..56ad1aeb57`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`cognition-core`/`swarm-protocol` package manifests are absent.
- Verified `56ad1aeb57..5e24d6ca21`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5e24d6ca21..dd66a388b2`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `dd66a388b2..be86b94ea4`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `be86b94ea4..be611ccb20`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `be611ccb20..b4f0bab20c`: no new README command-map additions were found (README changes only documented `retryBudget` orchestration controls); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b4f0bab20c..64ead56890`: no new README command-map additions were found (README changes only documented per-target circuit breaking/duplicate task-id rejection and retry backoff+jitter behavior); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `64ead56890..67d4f2d95b`: no new README command-map additions were found (README changes only documented orchestrator graceful drain-mode admission controls); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `67d4f2d95b..eb7d8e6ab6`: no new README command-map additions were found (README changes only documented drain-mode grace-timeout force cancellation controls: `forceCancelAfterMs` and `propagateCancel`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `eb7d8e6ab6..b17762e7d8`: no new README command-map additions were found (README changes only documented maintenance retry-budget controls: `maintenancePolicy.maxRetryDispatchesPerRun` and `fairRetryDispatchByTarget`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `b17762e7d8..de0d53f7a0`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `fa980cc80a..a709e82722`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `a709e82722..200d60bcc2`: added README command-map workflow for epsilon-Thompson autonomous run flags (`--selection-policy epsilon_ts`, `--thompson-exploration`, `--thompson-prior-alpha`, `--thompson-prior-beta`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `200d60bcc2..f23cdaeef3`: added README command-map workflow for sliding-window epsilon-Thompson autonomous run flags (`--selection-policy sw_epsilon_ts`, `--window-size`, `--thompson-exploration`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `f23cdaeef3..3dc8b339c5`: added README command-map workflow for KL-UCB autonomous run flags (`--selection-policy kl_ucb`, `--kl-ucb-confidence`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `3dc8b339c5..91d6ab6e32`: added README command-map workflow for change-detection UCB autonomous run flags (`--selection-policy cd_ucb`, `--cd-min-samples`, `--cd-threshold`, `--cd-delta`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `91d6ab6e32..9e00e8aaa5`: added README command-map workflow for EXP3 policy corralling autonomous run flags (`--selection-policy corral_exp3`, `--corral-gamma`, `--corral-eta`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `bc50bbb952..7837b15052`: added README command-map workflows for discounted autonomous run flags (`--selection-policy d_ucb|d_epsilon_ts`, `--discount-factor`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `7837b15052..914e400a9d`: no new README command-map additions were found; `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `914e400a9d..d691975a9a`: added README command-map workflow for anytime-MOSS autonomous run flags (`--selection-policy moss_anytime`, `--moss-alpha`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `d691975a9a..7b3cb2292c`: no new README command-map additions were found (range contains AGENTS-only verification bookkeeping); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `7b3cb2292c..3c0f9f7d82`: added README command-map workflow for contextual LinUCB autonomous run flags (`--selection-policy linucb`, `--linucb-alpha`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `3c0f9f7d82..ef461262a0`: added README command-map workflow for discounted contextual LinUCB autonomous run flags (`--selection-policy d_linucb`, `--discount-factor`, `--linucb-alpha`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `ef461262a0..53323094c7`: no new README command-map additions were found (range adds warfighter skill/reference content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `4aa0f8609d..0e18e8f75a`: added README command-map workflows for linear Thompson autonomous run flags (`--selection-policy lints|d_lints`, `--lints-alpha`, `--discount-factor`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `0e18e8f75a..5b6507b72f`: added README command-map workflows for variance-aware UCB-Tuned autonomous run flags (`--selection-policy ucb_tuned|d_ucb_tuned`, `--discount-factor`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5b6507b72f..024d118261`: added README command-map workflows for Bayes-UCB autonomous run flags (`--selection-policy bayes_ucb|sw_bayes_ucb|d_bayes_ucb`, `--bayes-ucb-quantile`, `--window-size`, `--discount-factor`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `024d118261..c512bf151e`: added README command-map workflow for EXP3-IX autonomous run flags (`--selection-policy exp3_ix`, `--exp3-ix-gamma`, `--exp3-ix-eta`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `c512bf151e..e5b014bd87`: added README command-map workflow for CUSUM-UCB autonomous run flags (`--selection-policy cusum_ucb`, `--cd-min-samples`, `--cusum-threshold`, `--cusum-baseline-weight`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `e5b014bd87..64243f88ec`: no new README command-map additions were found (range adds warfighter skill/reference content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `64243f88ec..f53642dc5f`: added README command-map workflow for variance-aware UCB-V autonomous run flag (`--selection-policy ucb_v`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `885b0af4fa..10a934ed6b`: no new README command-map additions were found (range updates graded terminal reward learning behavior only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `10a934ed6b..6a8f614b61`: no new README command-map additions were found (range adds AGENTS bookkeeping only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `6a8f614b61..63f79a85da`: added README command-map workflow for sliding-window UCB-Tuned autonomous run flags (`--selection-policy sw_ucb_tuned`, `--window-size`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `63f79a85da..51b8990137`: added README command-map workflows for variance-aware UCB-V autonomous run flags (`--selection-policy ucb_v|sw_ucb_v|d_ucb_v`, `--ucb-v-exploration`, `--window-size`, `--discount-factor`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `51b8990137..5f8050e661`: added README command-map workflows for sliding-window contextual autonomous run flags (`--selection-policy sw_linucb|sw_lints`, `--window-size`, `--linucb-alpha`, `--lints-alpha`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `5f8050e661..547bf2ed6e`: no new README command-map additions were found (range adds warfighter skill/reference content only); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `547bf2ed6e..56d4dcf347`: no new README command-map additions were found (range is a merge commit without README command-map updates); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `56d4dcf347..6c7394aaae`: added README command-map workflow for expanded EXP3 policy corralling autonomous run flags (`--selection-policy corral_exp3_plus`, `--corral-gamma`, `--corral-eta`); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
- Verified `1d3941f8d9..98891794db`: no new README command-map additions were found (range improves reward-aware recency scoring + EXP3-IX recency metadata alignment); `package.json` script-map diff remains blocked in this checkout because root/`swarm-protocol` package manifests are absent.
