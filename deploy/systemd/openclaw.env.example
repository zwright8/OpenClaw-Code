# Required for signed operator actions in swarm-protocol ops commands.
SWARM_AUDIT_SECRET=replace-with-a-long-random-secret

# Optional path used by ancillary health scripts if you wire them in later.
# OPENCLAW_GATEWAY_LOG=/var/lib/openclaw/logs/gateway.log
